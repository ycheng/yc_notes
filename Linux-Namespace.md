
Network
* \# ip netns add ns1
* \# ip netns [list]
* \# ip netns del ns1
* \# ip netns exec ns1 bash
* directory
  * /proc/[pid]/ns: the namespace a process blongs
  * /var/run/netns/: all network namespace.
